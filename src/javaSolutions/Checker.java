import java.util.Comparator;

/*
class Checker implements Comparator<Player> {
    // complete this method
    public int compare(Player a, Player b) {
        int comparison = Integer.compare(a.score, b.score) * -1;
        if (comparison == 0)
            return Character.compare(a.name.charAt(0), b.name.charAt(0));
        return comparison;
    }
}
*/
